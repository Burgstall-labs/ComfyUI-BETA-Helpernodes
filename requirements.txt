# requirements.txt for ComfyUI-BETA-Helpernodes

# Numpy is often used alongside torch
numpy

# Required for SharpestFrameClipper node
opencv-python

# Required for Scene Detection node
scenedetect[opencv]

# --- External System Dependencies (Not managed by pip) ---
# ... (Keep the FFmpeg note for MP3 saving) ...